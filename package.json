{
  "name": "viewport-update",
  "version": "0.0.0",
  "description": "Throttled window scroll and resize events. In the singleton + signal way",
  "repository": "jeromedecoster/canvas-funcs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT"
}
